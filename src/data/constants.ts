// Access code for the seeded sample report (see api/seed.ts). Shared between client and server
// so the "SAMPLE REPORT" banner can detect it without hardcoding the code in two places.
export const SAMPLE_REPORT_UID = 'CQ7D4DM3'
